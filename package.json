{
  "name": "retail-sales-management-system",
  "version": "1.0.0",
  "description": "A comprehensive Retail Sales Management System with search, filter, sort, and pagination",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run build && npm run preview",
    "seed": "cd backend && npm run seed"
  },
  "keywords": ["retail", "sales", "management", "react", "nodejs", "mongodb"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
